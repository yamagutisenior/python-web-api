[default]
secret_key = "ThisIsARandomSecretKey"  #PARA GERAR UMA PSEUDO SECRETKEY USER O COMANDO >> python3 -c "import secrets; print(secrets.token_hex())"
mongo_uri = "mongodb://localhost:27017/blog"
title = "Meu Blog"
extensions = [
    "blog.database:configure",
    "blog.commands:configure",
    "flask_bootstrap:Bootstrap",
    "blog.views:configure",
    "blog.plugins:configure",
    "blog.auth:configure",
    "blog.admin:configure",
]
